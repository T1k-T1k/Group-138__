1. 
На Windows код отлечаеться от кода на Mac, там команды некоторые другие, и даже команду doskey /history нельзя ввести в консоли
и надо создавать отдельный файл где то, чтобы в нем написало историю команд (В архиве есть файл history там есть история команд
для дз из битрута)

2.
Я еще сделал дз которое не от битрута а от вас и код можно посмотреть как раз таки 
на гитхабе: https://github.com/T1k-T1k/Registry-Site.git
или же напрямую в этом архиве ;-;
